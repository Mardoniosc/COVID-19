import { BrasilRouting.Module } from './brasil-routing.module';

describe('BrasilRouting.Module', () => {
  it('should create an instance', () => {
    expect(new BrasilRouting.Module()).toBeTruthy();
  });
});
